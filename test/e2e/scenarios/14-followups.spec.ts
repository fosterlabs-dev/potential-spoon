/**
 * Section 14 — Follow-up sequences.
 *
 * The follow-ups module is Phase 3 (🔴 not yet implemented per CLAUDE.md).
 * Tests staged as todos so they appear in the run and prompt implementation.
 */
describe('Scenario 14 — Follow-up sequences (Phase 3 — not yet implemented)', () => {
  it.todo('14.1 — 24h follow-up after a quote with no reply');
  it.todo('14.2 — 7-day follow-up after no reply');
  it.todo('14.3 — no message after 7-day follow-up; status → Lost after grace period');
  it.todo('14.4 — customer reply between 24h and 7d cancels the sequence');
  it.todo('14.5 — customer book between 24h and 7d cancels the sequence');
});
