/**
 * Section 15 — SuperControl email nudges.
 *
 * The email-integration module is Phase 5 (🔴 not yet implemented per CLAUDE.md).
 */
describe('Scenario 15 — SuperControl email nudges (Phase 5 — not yet implemented)', () => {
  it.todo('15.1 — booking confirmation email triggers nudge_booking_confirmation');
  it.todo('15.2 — arrival details email triggers nudge_directions');
  it.todo('15.3 — pre-arrival reminder email triggers nudge_pre_arrival');
  it.todo('15.4 — check-in + 1 day cron triggers nudge_mid_stay');
  it.todo('15.5 — thank-you email triggers nudge_thank_you');
  it.todo('15.6 — review-request email triggers nudge_review_request');
  it.todo('15.7 — duplicate booking confirmation does not double-send');
  it.todo('15.8 — unmatched email recipient notifies Jim, no WA send');
});
